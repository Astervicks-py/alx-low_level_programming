#include "main.h"

/**
 * _isalpha - print a bunch of stuff that i am too lazy type
 * @c: also print things
 * Return: returns stuff
 */
int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
}
